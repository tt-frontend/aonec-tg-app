import { LoginPage } from "./LoginPage";

// import { loginService } from './loginService.models';
// const { inputs, outputs } = loginService;

export const LoginContainer = () => {
  return (
    <>
      <LoginPage />
    </>
  );
};
